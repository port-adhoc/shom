{
	"name": "port-adhoc/shom",
	"description": "Make a call to the shom text render api to collect data, put it in form and let you save it to a JSON file.",
	"type": "library",
	"license": "MIT",
	"minimum-stability": "stable",
	"require": {
		"php": ">=7.2.0",
		"guzzlehttp/guzzle": "6.*"
	},
	"autoload": {
		"psr-4": {
			"PortAdhoc\\": "src"
		}
	},
	"scripts": {
		"test": "vendor/bin/phpunit test"
	},
	"require-dev": {
		"phpunit/phpunit": "8.*"
	}
}
